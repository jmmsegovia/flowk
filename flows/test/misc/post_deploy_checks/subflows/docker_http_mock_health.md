# docker_http_mock_health

Detailed Description
Run a local HTTP echo server that returns a simple health payload. Primary actions: Docker container management, controlled waits.

Requirements
- Docker installed and daemon running.
