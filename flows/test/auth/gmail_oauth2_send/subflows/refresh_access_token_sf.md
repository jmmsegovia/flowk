# refresh_access_token_sf

Detailed Description
Refresh access token from refresh token. Primary actions: HTTP/HTTPS requests, variable configuration.

Requirements
- HTTP connectivity to the configured endpoint(s).
- Internet access for external HTTP/HTTPS endpoints.
