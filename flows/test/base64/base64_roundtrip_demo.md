# base64_roundtrip_demo

Detailed Description
Encodes and decodes a payload using BASE64, then validates the roundtrip result. Primary actions: Base64 encode/decode, assertions and conditional logic, console output, variable configuration.

Requirements
- `base64` command available in PATH.
