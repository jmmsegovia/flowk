# subflows.demo_musicbrainz_artist_asserts

Detailed Description
Query MusicBrainz and validate the artist payload with rich assertions. Primary actions: HTTP/HTTPS requests, parallel execution, console output, variable configuration.

Requirements
- HTTP connectivity to the configured endpoint(s).
