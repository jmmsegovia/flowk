# ssh_command_demo

Detailed Description
Demonstrates running remote commands over SSH. Imports 1 subflow(s): docker_ssh_local. Primary actions: Docker container management, controlled waits, SSH operations.

Requirements
- Docker installed and daemon running.
- Reachable SSH server and credentials/keys configured.
