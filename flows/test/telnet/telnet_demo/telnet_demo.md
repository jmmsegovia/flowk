# telnet_demo

Detailed Description
Sample Telnet session that logs into a device and captures the version banner. Imports 1 subflow(s): docker_telnet_local. Primary actions: Docker container management, controlled waits, Telnet checks.

Requirements
- Docker installed and daemon running.
- Reachable Telnet server.
