# helm_release_lifecycle_demo

Detailed Description
Demonstrates repository management, chart search, install/status/history, and cleanup with the HELM action. Primary actions: Helm operations, shell command execution, variable configuration.

Requirements
- Helm CLI available in PATH and cluster access.
- Shell access and the `date` command available.
