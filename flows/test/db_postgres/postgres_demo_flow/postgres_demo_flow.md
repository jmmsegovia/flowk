# postgres_demo_flow

Detailed Description
Sample flow for Postgres actions. Imports 1 subflow(s): docker_postgres_local. Primary actions: PostgreSQL operations, Docker container management, controlled waits, variable configuration.

Requirements
- Docker installed and daemon running.
- Reachable PostgreSQL instance and valid credentials.
